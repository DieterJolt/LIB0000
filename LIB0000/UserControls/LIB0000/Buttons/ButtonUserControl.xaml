<UserControl x:Class="LIB0000.ButtonUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:LIB0000"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">
    <Grid >
        <Button x:Name="Button" HorizontalContentAlignment="Stretch" 
                VerticalContentAlignment="Stretch" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                Background="LightGray" Click="Button_Click" PreviewMouseLeftButtonUp="Button_PreviewMouseLeftButtonUp"
                BorderBrush="Gray" BorderThickness="3" PreviewMouseLeftButtonDown="Button_PreviewMouseLeftButtonDown"
                MouseLeave="Button_MouseLeave"
                Width="Auto" Height="Auto" MaxHeight="50" MaxWidth="50" Margin="10,20,0,20">

            <fa:IconImage Icon="{Binding Icon,RelativeSource={RelativeSource AncestorType=UserControl}}" 
                          Width="Auto" Height="Auto" 
                          Foreground="{Binding IconColor,RelativeSource={RelativeSource AncestorType=UserControl}}" 
                          VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

        </Button>
    </Grid>
</UserControl>
