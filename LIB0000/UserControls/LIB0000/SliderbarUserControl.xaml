<UserControl x:Class="LIB0000.SliderbarUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LIB0000"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
    <Grid>
        <Border Grid.Row="1" HorizontalAlignment="Stretch" x:Name="MySliderbar" CornerRadius="20" BorderThickness="5" BorderBrush="{Binding ColorBorder,RelativeSource={RelativeSource AncestorType=UserControl}}" Background="{Binding ColorBackground,RelativeSource={RelativeSource AncestorType=UserControl}}"  
                ClipToBounds="True" VerticalAlignment="Stretch" Margin="10">

            <Grid Width="{Binding ActualWidth, ElementName=MySliderbar}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding TitleName,RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="White" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" FontSize="{DynamicResource LargeFontSize}"/>

                <Canvas x:Name="TrackCanvas" Grid.Row="1" Grid.Column="1"  Height="8" HorizontalAlignment="Stretch" Margin="0,0,0,25"/>

                 <!--Slider-->
                <Slider x:Name="MySlider" Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="{Binding MaxValue,RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{Binding SliderValue, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                        TickPlacement="BottomRight"  Ticks="{Binding SliderTicks,RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Loaded="Slider_Loaded"
                        ValueChanged="Slider_ValueChanged" HorizontalAlignment="Stretch"   >
                    <Slider.Template>
                        <ControlTemplate TargetType="Slider">
                            <Grid>
                                <Track x:Name="PART_Track">
                                    <Track.Thumb>
                                        <Thumb Width="20" Height="20" Margin="0,0,0,25">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Grid>
                                                        <Border 
                                                            Background="White"
                                                            CornerRadius="10"
                                                            Width="20" 
                                                            Height="20"/>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Slider.Template>
                </Slider>

                <Grid Margin="0" Grid.Row="2" Grid.Column="1">

                    <TextBlock x:Name="MyTextBlock" Text="{Binding SliderValue,RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold" FontSize="{DynamicResource MediumTitleFontSize}"  VerticalAlignment="Center"/>
                    <!--<TextBlock Grid.Column="0" Foreground="White" Text="Value :" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" Width="125" Text="{Binding SliderValue,RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat=F2}" HorizontalContentAlignment="Center" HorizontalAlignment="Left"/>-->

                </Grid>

            </Grid>

        </Border>
    </Grid>
</UserControl>
