<Page x:Class="LIB0000.EmptyIndexView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 

      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmptyIndexView"> 

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Click="Button_Click" Grid.Column="0" Grid.Row="0">Zet Output C2 hoog</Button>
        <Button Click="Button_Click2" Grid.Column="1" Grid.Row="0">Zet Output C2 laag</Button>

        <Button Click="Button_Click3" Grid.Column="0" Grid.Row="1">Zet Output C3 hoog</Button>
        <Button Click="Button_Click4" Grid.Column="1" Grid.Row="1">Zet Output C3 laag</Button>

        <Button Grid.Column="0" Grid.Row="2" x:Name="C0" Content="{Binding BasicService.TurckService[0].InputStates[0]}"></Button>

        <Button Grid.Column="0" Grid.Row="3" x:Name="C1" Content="{Binding BasicService.TurckService[0].InputStates[1]}"></Button>


    </Grid>
</Page>
