<Page x:Class="LIB0000.FunctionHalconShapeSearchView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ha="http://schemas.mvtec.com/halcondotnet"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FunctionHalconShapeSearchView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ha:HSmartWindowControlWPF Name="WindowControl" 
                                                 Grid.Row="1" Grid.Column="1">
            <ha:HIconicDisplayObjectWPF IconicObject="{Binding BasicService.HalconServiceSelected.Grab.Image}"/>
            <!--<ha:HIconicDisplayObjectWPF IconicObject="{Binding BasicService.HalconServiceSelected.ShapeSearch.ResultRegion}" HColor="green" />-->
        </ha:HSmartWindowControlWPF>
        <Grid>
            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Test" Command="{Binding BasicService.HalconServiceSelected.cmdTeachCommand }"/>
            <Button Grid.Column="1" Content="Camera 1" Click="Button_Click"/>
            <Button Grid.Column="2" Content="Camera 2" Click="Button02_Click"/>
            <TextBlock Grid.Column="3" Foreground="White" Text="{Binding BasicService.HalconServiceSelected.TestCounter}"/>
        </Grid>

    </Grid>
</Page>
