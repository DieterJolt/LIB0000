<Page x:Class="LIB0000.OrderHistoryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderHistoryView">

    <Grid x:Name="Maingrid" Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition x:Name="row0" Height="1*"/>
            <RowDefinition x:Name="row1" Height="1*"/>
            <RowDefinition x:Name="row2" Height="6*"/>
            <RowDefinition x:Name="row3" Height="1*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="gridRow0" Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!--<local:StatusBorderTextValueUserControl Grid.Column="0" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Order" TextBottom="{Binding BasicService.OrdersService.Order.Selected.OrderNr}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="1" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Producten totaal" TextBottom="{Binding BasicService.OrdersService.Order.Selected.TotalProduct}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="2" Color="{StaticResource ColorRed}" ColorText="White" TextTop="Product afkeur" TextBottom="{Binding BasicService.OrdersService.Order.Selected.BadProduct}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="3" Color="{StaticResource ColorGreen}" ColorText="White" TextTop="Product ok" TextBottom="{Binding BasicService.OrdersService.Order.Selected.GoodProduct}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="4" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Begin order" TextBottom="{Binding BasicService.OrdersService.Order.Selected.DateTimeStart}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="5" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Einde order" TextBottom="{Binding BasicService.OrdersService.Order.Selected.DateTimeStop}"/>-->

        </Grid>

        <Grid x:Name="gridRow1" Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <ui:ToggleSwitch IsChecked="{Binding BasicService.OrdersService.Order.FilterStoppedByCamera}" Content="Camera detectie" Foreground="White" Margin="10"/>
                <ui:ToggleSwitch IsChecked="{Binding BasicService.OrdersService.Order.FilterTimeouts}" Content="TimeOut" Foreground="White" Margin="10"/>
                <ui:ToggleSwitch IsChecked="{Binding BasicService.OrdersService.Order.FilterInstruction}" Content="Instructies" Foreground="White" Margin="10"/>
                <ui:ToggleSwitch IsChecked="{Binding BasicService.OrdersService.Order.FilterStartStop}" Content="Start / Stop" Foreground="White" Margin="10"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="gridRow2" Grid.Row="2" Background="#202020" Margin="0,0,10,0" MaxHeight="500">

            <!--<Border BorderBrush="DimGray" BorderThickness="2">
                <ScrollViewer x:Name="ScrollViewer" VerticalScrollBarVisibility="Visible"  HorizontalScrollBarVisibility="Disabled">
                    <ListBox x:Name="MyListBox" ItemsSource="{Binding BasicService.OrdersService.Order.ListSelectedFiltered, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                SelectedItem="{Binding BasicService.InstructionsService.InstructionHistory.OrderHistorySelected,Mode=TwoWay}" IsEnabled="True"
                                SelectionChanged="ListBox_SelectionChanged">

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                --><!--<local:OrderUserControl OrderHistory="{Binding}"
                                                     cmdNavigateToInstructionHistory="{Binding DataContext.cmdNavigateToInstructionHistoryCommand, RelativeSource={RelativeSource AncestorType=Page}}"/>--><!--
                            </DataTemplate>
                        </ListBox.ItemTemplate>

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" >
                                <Style.Setters>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style.Setters>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </ScrollViewer>
            </Border>-->
        </Grid>

        <Grid x:Name="gridButtons" Grid.Row="3">
            <!--<local:Footer5ButtonsUserControl VerticalAlignment="Center"    
                                                Button1Text="{DynamicResource Terug}"
                                                Button1Image="/Assets/Images/Return.png"
                                                Button1Visibility="True"
                                                Button1Command="{Binding cmdBackCommand}"/>-->
        </Grid>
    </Grid>
</Page>
