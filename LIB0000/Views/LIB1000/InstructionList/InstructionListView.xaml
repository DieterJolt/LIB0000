<Page x:Class="LIB0000.InstructionListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"  
      Title="InstructionListView" Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Grid>
        <Grid x:Name="gridMain" Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid x:Name="gridSearch" Grid.Row="0">
                <ui:AutoSuggestBox Grid.Column="1" x:Name="AutoSuggestBox" TextChanged="AutoSuggestBox_TextChanged" />
            </Grid>
            <Grid x:Name="gridDatagrid" Grid.Row="1">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <DataGrid HeadersVisibility="Column" Grid.Column="0" MaxHeight="{Binding ActualHeight, ElementName=gridDatagrid}"
                             ItemsSource="{Binding BasicService.InstructionsService.InstructionLists.List ,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"
                             SelectedValue="{Binding BasicService.InstructionsService.InstructionLists.Selected}" 
                             AutoGenerateColumns="False" 
                             SelectionChanged="DataGrid_SelectionChanged" >


                    <DataGrid.Columns>
                        <DataGridTextColumn Visibility="Hidden" Binding="{Binding Id }" >
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="Id"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Name}">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelNaam}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Description}">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelBeschrijving}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding CurrentVersion}">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelHuidigeVersie}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>

                    </DataGrid.Columns>


                </DataGrid>

            </Grid>
            <Grid x:Name="gridButtons" Grid.Row="2">

                <local:Footer10ButtonsUserControl VerticalAlignment="Center"
                    
                    Button1Text="{DynamicResource Toevoegen}"
                    Button1Image="pack://application:,,,/Assets/Images/Plus.png"
                    Button1Visibility="True"
                    Button1Command="{Binding cmdAddRowCommand}"
                    
                    Button2Text="{DynamicResource Verwijderen}"
                    Button2Image="pack://application:,,,/Assets/Images/Delete.png"
                    Button2Visibility="True"
                    Button2Command="{Binding cmdDeleteRowCommand}"
                    
                    Button6Text="{DynamicResource Bewerken}"
                    Button6Image="pack://application:,,,/Assets/Images/Edit.png"
                    Button6Visibility="True"
                    Button6Command="{Binding cmdEditRowCommand}"       
                                                  
                    Button7Text="{DynamicResource Detail}"
                    Button7Image="pack://application:,,,/Assets/Images/Detail.png"
                    Button7Visibility="True"
                    Button7Command="{Binding cmdDetailCommand}"       
                                                  
                    Button3Text="Copy"
                    Button3Image="pack://application:,,,/Assets/Images/Copy.png"
                    Button3Visibility="True"
                    Button3Command="{Binding cmdCopyCommand}"   
                    />

            </Grid>
        </Grid>
    </Grid>
</Page>
