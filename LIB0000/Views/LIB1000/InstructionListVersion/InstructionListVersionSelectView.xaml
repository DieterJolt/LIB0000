<Page x:Class="LIB0000.InstructionListVersionSelectView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InstructionListVersionSelectView" Loaded="Page_Loaded">



    <Grid x:Name="gridMain" Grid.Row="1" Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="11*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!--<Grid x:Name="gridSearch" Grid.Row="0">
            <ui:AutoSuggestBox Grid.Column="1" x:Name="AutoSuggestBox" TextChanged="AutoSuggestBox_TextChanged" />
        </Grid>-->
        <Grid x:Name="gridDatagrid" Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding BasicService.InstructionsService.InstructionLists.ListVersions ,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                        SelectedValue="{Binding BasicService.InstructionsService.InstructionLists.SelectedVersion}" SelectionChanged="DataGrid_SelectionChanged" >
                <DataGrid.Columns>
                    <DataGridTextColumn Visibility="Hidden" Binding="{Binding Id }" Width="30">
                        <DataGridTextColumn.Header>
                            <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="Id"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Version}" Width="250">
                        <DataGridTextColumn.Header>
                            <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelVersie}"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Approved}" Width="250">
                        <DataGridTextColumn.Header>
                            <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelGoedgekeurd}"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding DateTimeApproved}" Width="250">
                        <DataGridTextColumn.Header>
                            <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelGoedgekeurdop}"/>
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>

                </DataGrid.Columns>

            </DataGrid>

        </Grid>
        <Grid x:Name="gridButtons" Grid.Row="2">

            <local:Footer10ButtonsUserControl VerticalAlignment="Center"

                    Button1Text="{DynamicResource Ok}"
                    Button1Image="pack://application:,,,/Assets/Images/Yes.png"
                    Button1Visibility="True"
                    Button1Command="{Binding cmdOkCommand}"                                              
                    />

        </Grid>

    </Grid>


</Page>
