<Page x:Class="LIB0000.InstructionTypeSelectView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InstructionTypeSelectView" Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Grid>

        <Grid x:Name="gridMain" Grid.Row="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <Grid x:Name="Inputfield" Grid.Column="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="11*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="5"/>
                </Grid.RowDefinitions>

                <ui:AutoSuggestBox x:Name="AutoSuggestBox" Grid.Column="1" Grid.Row="1"
                    TextChanged="AutoSuggestBox_TextChanged"/>

            </Grid>

            <Grid x:Name="DataGrid" Grid.Row="1">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="11*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <DataGrid HeadersVisibility="Column" Grid.Column="1" Height="600"
         ItemsSource="{Binding BasicService.InstructionsService.InstructionTypes.List}"
         SelectedValue="{Binding BasicService.InstructionsService.InstructionTypes.Selected}" >


                    <DataGrid.Columns>
                        <DataGridTextColumn Visibility="Hidden" Binding="{Binding Id }" Width="Auto">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="Id"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Width="150">
                            <DataGridTemplateColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelIcoon}"/>
                            </DataGridTemplateColumn.Header>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding Icon}"  Stretch="Uniform"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Binding="{Binding Name}" Width="375">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelBeschrijving}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>

                    </DataGrid.Columns>


                </DataGrid>

            </Grid>

            <Grid x:Name="Button" Grid.Row="2">


                <local:Footer10ButtonsUserControl VerticalAlignment="Center"

                    Button1Text="OK"
                    Button1Image="pack://application:,,,/Assets/Images/yes.png"
                    Button1Visibility="True"
                    Button1Command="{Binding cmdSelectInstructionOKCommand}"

                    Button2Text="{DynamicResource Annuleer}"
                    Button2Image="pack://application:,,,/Assets/Images/delete.png"
                    Button2Visibility="True"
                    Button2Command="{Binding cmdBackToInstructionEditCommand}"       

                    />

            </Grid>

        </Grid>

    </Grid>
</Page>
