<Page x:Class="LIB0000.InstructionHistoryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InstructionView" >

    <Page.Resources>
        <DataTemplate x:Key="EmptyTemplate">
            <!--Lege template-->
        </DataTemplate>
        <DataTemplate x:Key="ClickTemplate">
            <local:LayoutEdit01ClickUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False" NextButtonVisibilityMode="Collapsed"/>
        </DataTemplate>

        <DataTemplate x:Key="HotspotTemplate">
            <local:LayoutEdit02HotspotUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False"/>
        </DataTemplate>

        <DataTemplate x:Key="SnapshotTemplate">
            <local:LayoutEdit03SnapshotUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False" NextButtonVisibilityMode="Collapsed"/>
        </DataTemplate>

        <DataTemplate x:Key="RemoteInputTemplate">
            <local:LayoutEdit04RemoteInputUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False" NextButtonVisibilityMode="Collapsed"/>
        </DataTemplate>

        <DataTemplate x:Key="ScanTemplate">
            <local:LayoutEdit05ScanUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.asicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False"/>
        </DataTemplate>

        <DataTemplate x:Key="ChecklistTemplate">
            <local:LayoutEdit06ChecklistUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False" NextButtonVisibilityMode="Collapsed"/>
        </DataTemplate>

        <DataTemplate x:Key="ImageChecklistTemplate">
            <local:LayoutEdit07ImageChecklistUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False" NextButtonVisibilityMode="Collapsed"/>
        </DataTemplate>

        <DataTemplate x:Key="SelectionListTemplate">
            <local:LayoutEdit08SelectionlistUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False" NextButtonVisibilityMode="Collapsed"/>
        </DataTemplate>

        <DataTemplate x:Key="ImageSelectionListTemplate">
            <local:LayoutEdit09ImageSelectionListUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False" NextButtonVisibilityMode="Collapsed"/>
        </DataTemplate>

        <DataTemplate x:Key="YesNoTemplate">
            <local:LayoutEdit11YesNoUserControl InstructionModel="{Binding}" 
                                                cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                                   RelativeSource={RelativeSource AncestorType=Page}}"
                                                EditVisibilityMode="Visible" EditEnabledMode="False"/>
        </DataTemplate>

        <DataTemplate x:Key="PdfTemplate">
            <local:LayoutEdit12PdfUserControl InstructionModel="{Binding}" 
                                        cmdInstructionOk="{Binding DataContext.BasicService.OrdersService.Order.cmdInstructionOkCommand,
                                                           RelativeSource={RelativeSource AncestorType=Page}}"
                                        EditVisibilityMode="Visible" EditEnabledMode="False" NextButtonVisibilityMode="Collapsed"/>
            
        </DataTemplate>

        <local:InstructionTypeDataTemplate x:Key="InstructionTypeDataTemplate"
                                            EmptyTemplate="{StaticResource EmptyTemplate}"
                                            ClickTemplate="{StaticResource ClickTemplate}"
                                            HotspotTemplate="{StaticResource HotspotTemplate}"
                                            SnapshotTemplate="{StaticResource SnapshotTemplate}"
                                            RemoteInputTemplate="{StaticResource RemoteInputTemplate}"
                                            ScanTemplate="{StaticResource ScanTemplate}"
                                            ChecklistTemplate="{StaticResource ChecklistTemplate}"
                                            ImageChecklistTemplate="{StaticResource ImageChecklistTemplate}"
                                            SelectionListTemplate="{StaticResource SelectionListTemplate}"
                                            ImageSelectionListTemplate="{StaticResource ImageSelectionListTemplate}"
                                            YesNoTemplate="{StaticResource YesNoTemplate}"
                                            PdfTemplate="{StaticResource PdfTemplate}"/>

    </Page.Resources>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <ContentControl Grid.Row="0" Content="{Binding BasicService.InstructionsService.InstructionHistory.HistorySelected}" 
                        ContentTemplateSelector="{StaticResource InstructionTypeDataTemplate}" />

        <Grid Grid.Row="1">

            <local:Footer5ButtonsUserControl
                
                Button1Text="Back"
                Button1Image="/Assets/Images/Return.png"
                Button1Command="{Binding cmdGoBackCommand}"
                Button1Visibility="True"
                
                />

        </Grid>
        
    </Grid>
</Page>
