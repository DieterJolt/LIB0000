<Page x:Class="LIB0000.WorkstationSelectView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WorkstationSelectView" Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Grid >

        <Grid x:Name="gridMain" Grid.Row="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="7*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="11*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ui:AutoSuggestBox Grid.Column="1" x:Name="AutoSuggestBox" PlaceholderText="Zoek een Workstation" TextChanged="AutoSuggestBox_TextChanged"/>

            </Grid>

            <Grid x:Name="gridDatagrid" Grid.Row="1">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="11*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ui:DataGrid Grid.Column="1" MaxHeight="{Binding ActualHeight, ElementName=gridDatagrid}" ItemsSource="{Binding BasicService.WorkstationsService.Workstations.List ,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"
                  SelectedValue="{Binding BasicService.WorkstationsService.Workstations.Selected}" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="DataGrid_SelectionChanged">

                    <!-- Define columns -->
                    <ui:DataGrid.Columns>
                        <DataGridTextColumn Visibility="Hidden" Binding="{Binding Id }">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="Id"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Name}">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelNaam}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Description}">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelBeschrijving}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>

                    </ui:DataGrid.Columns>

                    <ui:DataGrid.Resources>
                        <Style TargetType="ScrollBar">
                            <Setter Property="Width" Value="25" />
                        </Style>

                    </ui:DataGrid.Resources>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <!-- Trigger to change style when row is selected -->
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource ColorRed}"/>
                                    <!-- Change background color when selected -->
                                    <Setter Property="Foreground" Value="White"/>
                                    <!-- Change foreground color when selected -->
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                </ui:DataGrid>

            </Grid>

            <Grid x:Name="Buttons" Grid.Row="2">

                <!--<Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1" HorizontalAlignment="Stretch" Command="{Binding cmdAddInstructionCommand}">
                    <Grid Width="100">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6*"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="pack://application:,,,/Assets/Images/Plus.png"  MaxWidth="15" Grid.Column="0"/>
                        <TextBlock Text="OK" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="2" />

                    </Grid>
                </Button>

                <Button Grid.Column="3" HorizontalAlignment="Stretch" Command="{Binding cmdDeleteInstructionCommand}">
                    <Grid Width="100">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>

                            <ColumnDefinition Width="6*"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="pack://application:,,,/Assets/Images/Delete.png" MaxWidth="15" Grid.Column="0"/>
                        <TextBlock Text="{DynamicResource Annuleer}" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="2" />

                    </Grid>
                </Button>-->

                <local:Footer5ButtonsUserControl VerticalAlignment="Center"
    
    Button1Text="OK"
    Button1Image="pack://application:,,,/Assets/Images/Yes.png"
    Button1Visibility="True"
    Button1Command="{Binding}"
    
    Button2Text="{DynamicResource Annuleer}"
    Button2Image="pack://application:,,,/Assets/Images/Delete.png"
    Button2Visibility="True"
    Button2Command="{Binding}"

    Button3Text="{DynamicResource Detail}"
    Button3Image="pack://application:,,,/Assets/Images/Edit.png"
    Button3Visibility="True"
    Button3Command="{Binding cmdNavigateToWorkstationEditViewCommand }"
    
    />

            </Grid>

        </Grid>



    </Grid>


</Page>
