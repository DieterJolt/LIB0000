<Page x:Class="LIB0000.UserSelectView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"  
      Title="UserSelectView" Unloaded="Page_Unloaded">

    <Grid >

        <Grid x:Name="gridMain" Grid.Row="1" Margin="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="7*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">

                <ui:AutoSuggestBox Grid.Column="1" x:Name="AutoSuggestBox" TextChanged="AutoSuggestBox_TextChanged"/>

            </Grid>

            <Grid x:Name="gridDatagrid" Grid.Row="1">

                <DataGrid Grid.Column="1" HeadersVisibility="Column" ItemsSource="{Binding BasicService.UsersService.User.List,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"  MaxHeight="{Binding ActualHeight, ElementName=gridDatagrid}"
                  SelectedValue="{Binding BasicService.UsersService.User.Selected}" AutoGenerateColumns="False" IsReadOnly="True">

                    <!-- Define columns -->
                    <DataGrid.Columns>
                        <DataGridTextColumn Visibility="Hidden" Binding="{Binding Id }">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource MediumFontSize}" Text="Id"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding User }">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource MediumFontSize}" Text="{DynamicResource TabelGebruiker}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Level }">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource MediumFontSize}" Text="{DynamicResource TabelNiveau}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Password }" Visibility="Hidden">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource MediumFontSize}" Text="{DynamicResource TabelWachtwoord}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>

                    </DataGrid.Columns>
                </DataGrid>


            </Grid>

            <Grid x:Name="Buttons" Grid.Row="2">

                <local:Footer5ButtonsUserControl VerticalAlignment="Center"
 
                     Button1Text="OK"
                     Button1Image="pack://application:,,,/Assets/Images/Yes.png"
                     Button1Visibility="True"
                     Button1Command="{Binding cmdSelectUserCommand}"
 
                     Button2Text="{DynamicResource Annuleer}"
                     Button2Image="pack://application:,,,/Assets/Images/Delete.png"
                     Button2Visibility="True"
                     Button2Command="{Binding cmdGoBackCommand}"
 
                     />

            </Grid>

        </Grid>



    </Grid>
</Page>
