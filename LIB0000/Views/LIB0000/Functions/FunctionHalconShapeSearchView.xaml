<Page x:Class="LIB0000.FunctionHalconShapeSearchView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ha="http://schemas.mvtec.com/halcondotnet"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FunctionHalconShapeSearchView">

    <Grid x:Name="gridMain">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="gridPictures" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ha:HSmartWindowControlWPF x:Name="HSmartWindow01"
                                                 Grid.Row="0" Grid.Column="0" MouseWheel="HSmartWindow01_MouseWheel"  MouseDoubleClick="WindowControl_MouseDoubleClick">
                <!--<ha:HIconicDisplayObjectWPF IconicObject="{Binding BasicService.HalconService[0].GrabImage}"/>
            <ha:HIconicDisplayObjectWPF IconicObject="{Binding BasicService.HalconService[0].Contour}" HColor="green" LineWidth="1" />-->
                <!-- OCR tekstlabels -->
            </ha:HSmartWindowControlWPF>
        </Grid>


        
        <Grid x:Name="gridCommands" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Next" Command="{Binding BasicService.HalconService[0].cmdTeachA003Command }"/>
            <Button Grid.Column="1" Content="Detect" Command="{Binding BasicService.HalconService[0].cmdDetectA003Command }"/>
            <Label Grid.Column="2" HorizontalAlignment="Center" Content="{Binding BasicService.HalconService[0].A003CardCountingStat.CardsCountedResult}"/>
            <!--<TextBlock Grid.Column="3" Foreground="White" Text="{Binding BasicService.HalconService[0].TestCounter}"/>-->
        </Grid>

    </Grid>
</Page>
