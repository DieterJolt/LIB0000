<Page x:Class="LIB0000.OrderActualView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      x:Name="OrderActual"
      Title="OrderActual">


    <Grid x:Name="Maingrid" Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition x:Name="row0" Height="1*"/>
            <RowDefinition x:Name="row1" Height="1*"/>
            <RowDefinition x:Name="row2" Height="1*"/>
            <RowDefinition x:Name="row3" Height="1*"/>
            <RowDefinition x:Name="row4" Height="5*"/>
        </Grid.RowDefinitions>


        <Grid x:Name="gridRow0" Grid.Row="0" >
            <local:StatusBorderTextValueUserControl Grid.Column="0" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Order" TextBottom="{Binding BasicService.OrdersService.Order.Loaded.OrderNr, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
        </Grid>

        <Grid x:Name="gridRow1" Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:StatusBorderTextValueUserControl Grid.Column="1" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Product" TextBottom="{Binding BasicService.ProductsService.Product.Loaded.Name}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="0" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Productgroup" TextBottom="{Binding BasicService.ProductGroupsService.ProductGroup.Loaded.Name}"/>

        </Grid>
        <Grid x:Name="gridRow2" Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:StatusBorderTextValueUserControl Grid.Column="0" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Producten totaal" TextBottom="{Binding BasicService.OrdersService.Order.Loaded.TotalProduct}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="1" Color="{StaticResource ColorRed}" ColorText="White" TextTop="Product afkeur" TextBottom="{Binding BasicService.OrdersService.Order.Loaded.BadProduct}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="2" Color="{StaticResource ColorGreen}" ColorText="White" TextTop="Product ok" TextBottom="{Binding BasicService.OrdersService.Order.Loaded.GoodProduct}"/>


        </Grid>
        <!--<Grid x:Name="gridRow3" Grid.Row="3">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:StatusBorderTextValueUserControl Grid.Column="0" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Begin order" TextBottom="{Binding BasicService.OrdersService.Order.Loaded.DateTimeStart}"/>
            <local:StatusBorderTextValueUserControl Grid.Column="1" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Einde order" TextBottom=""/>
            <local:StatusBorderTextValueUserControl Grid.Column="2" Color="{StaticResource ColorRed}" ColorText="White" TextTop="Stops" TextBottom="{Binding BasicService.OrdersService.Order.Loaded.MachineStopAmount}"/>

        </Grid>-->
        <Grid x:Name="gridRow3" Grid.Row="3">
            <local:FhViewUserControl SelectedFhService="{Binding BasicService.FhService[0]}" SelectedHardwareFunction="{Binding BasicService.HardwareService.Hardware.List[2].LoadedFunction,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"/>
        </Grid>
        
    </Grid>

</Page>
