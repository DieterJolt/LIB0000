<Page x:Class="LIB0000.OrderActualCameraView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderActualCameraView">

    <Grid x:Name="Maingrid" Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition x:Name="row0" Height="1*"/>
            <RowDefinition x:Name="row1" Height="8*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="gridRow0" Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:StatusBorderTextValueUserControl Grid.Column="0" Color="{StaticResource ColorGray}" ColorText="White" TextTop="Order" TextBottom="{Binding BasicService.OrdersService.Order.Loaded.OrderNr}"/>
        </Grid>

        <Grid x:Name="gridRow1" Grid.Row="1">
            <local:FhViewUserControl SelectedFhService="{Binding BasicService.FhService[0]}" SelectedHardwareFunction="{Binding BasicService.HardwareService.Hardware.List[2].LoadedFunction,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"/>
        </Grid>

    </Grid>
</Page>
