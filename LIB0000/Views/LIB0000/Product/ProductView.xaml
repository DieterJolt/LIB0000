<Page x:Class="LIB0000.ProductView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductView" Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Grid>
        <Grid x:Name="gridMain" Margin="20" >
            
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" VerticalAlignment="Center">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="0" x:Name="ProductGroupFilter" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Label.Content>
                                <MultiBinding Converter="{StaticResource ProductGroupIdToNameMultiConverter}" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                    <Binding Path="BasicService.ProductsService.Product.Edit.ProductGroupId" />
                                    <Binding Path="BasicService.ProductGroupsService.ProductGroup.List" />
                                </MultiBinding>
                            </Label.Content>
                        </Label>
                        <Button Content="Kies product group" Command="{Binding cmdSelectProductGroupCommand}" Grid.Column="1"/>
                        <Button Grid.Column="2" Content="Alle product groepen" Command="{Binding cmdSelectAllProductGroupsCommand}"/>

                    </Grid>
                    

                    <ui:AutoSuggestBox Grid.Column="1"  x:Name="AutoSuggestBox" TextChanged="AutoSuggestBox_TextChanged" />

                </Grid>

            </Grid>

            <Grid x:Name="gridDatagrid" Grid.Row="1" >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <DataGrid HeadersVisibility="Column" Grid.Column="0" MaxHeight="{Binding ActualHeight, ElementName=gridDatagrid}"
                             ItemsSource="{Binding BasicService.ProductsService.Product.List ,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             SelectedValue="{Binding BasicService.ProductsService.Product.Selected}" 
                             AutoGenerateColumns="False" 
                             SelectionChanged="DataGrid_SelectionChanged" >

                    <DataGrid.Columns>
                        <DataGridTextColumn Visibility="Hidden" Binding="{Binding Id }">
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="Id"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Name}" >
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelNaam}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Description}" >
                            <DataGridTextColumn.Header>
                                <TextBlock FontSize="{DynamicResource LargeFontSize}" Text="{DynamicResource TabelBeschrijving}"/>
                            </DataGridTextColumn.Header>
                        </DataGridTextColumn>

                    </DataGrid.Columns>


                </DataGrid>

                <Image VerticalAlignment="Center" HorizontalAlignment="Stretch" 
                       Grid.Column="1"  Source="{Binding BasicService.ProductsService.Product.Selected.Image}" 
                       />

            </Grid>
            <Grid x:Name="gridButtons" Grid.Row="2">

                <local:Footer5ButtonsUserControl VerticalAlignment="Center"
                    
                    Button1Text="{DynamicResource Toevoegen}"
                    Button1Image="pack://application:,,,/Assets/Images/Plus.png"
                    Button1Visibility="True"
                    Button1Command="{Binding cmdAddRowCommand}"
                    
                    Button2Text="{DynamicResource Verwijderen}"
                    Button2Image="pack://application:,,,/Assets/Images/Delete.png"
                    Button2Visibility="True"
                    Button2Command="{Binding cmdDeleteRowCommand}"
                    
                    Button3Text="{DynamicResource Bewerken}"
                    Button3Image="pack://application:,,,/Assets/Images/Edit.png"
                    Button3Visibility="True"
                    Button3Command="{Binding cmdEditRowCommand}"  
                                                 
                    Button4Text="{DynamicResource Detail}"
                    Button4Image="pack://application:,,,/Assets/Images/Detail.png"
                    Button4Visibility="True"
                    Button4Command="{Binding cmdProductDetailCommand}" 
                    />

            </Grid>
        </Grid>
    </Grid>
    </Page>


