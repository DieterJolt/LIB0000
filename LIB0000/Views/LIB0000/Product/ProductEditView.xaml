<Page x:Class="LIB0000.ProductEditView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductEditView">

    <Grid x:Name="gridMain" Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="gridData" Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical">

                <local:TextInputTextUserControl 
                 TextLeft="{DynamicResource Naam}"
                 Input="{Binding BasicService.ProductsService.Product.Edit.Name,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 TextRight=""/>
                <local:TextInputTextUserControl 
                 TextLeft="{DynamicResource Beschrijving}"
                 Input="{Binding BasicService.ProductsService.Product.Edit.Description,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 TextRight=""/>

                <local:TextInputButtonUserControl
                    TextLeft="Productgroup :"
                    CommandRight="{Binding cmdSelectProductGroupCommand}"
                    TextRight=""
                    ImageRight="pack://application:,,,/Assets/Images/Products.png"
                    VisibilityRight="True"
                    >

                    <local:TextInputButtonUserControl.Input>
                        <MultiBinding Converter="{StaticResource ProductGroupIdToNameMultiConverter}" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                            <Binding Path="BasicService.ProductsService.Product.Edit.ProductGroupId" />
                            <Binding Path="BasicService.ProductGroupsService.ProductGroup.List" />
                        </MultiBinding>
                    </local:TextInputButtonUserControl.Input>

                </local:TextInputButtonUserControl>




            </StackPanel>

            <Image Grid.Row="0" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="10" 
              Source="{Binding BasicService.ProductsService.Product.Edit.Image}"
                MouseDown="Change_Image"
             Stretch="Uniform"/>

        </Grid>



        <local:Footer5ButtonsUserControl VerticalAlignment="Center" Grid.Row="1"
                 
                 Button1Text="{DynamicResource Ok}"
                 Button1Image="pack://application:,,,/Assets/Images/Yes.png"
                 Button1Visibility="True"
                 Button1Command="{Binding cmdOkCommand}"
                 
                 Button2Text="{DynamicResource Annuleer}"
                 Button2Image="pack://application:,,,/Assets/Images/Delete.png"
                 Button2Visibility="True"
                 Button2Command="{Binding cmdCancelCommand}"
                                                                                                           
                 />


    </Grid>
</Page>
