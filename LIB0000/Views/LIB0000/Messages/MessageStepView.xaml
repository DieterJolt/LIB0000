<Page x:Class="LIB0000.MessageStepView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      DataContext = "{Binding RelativeSource={RelativeSource Self}}"
      xmlns:local="clr-namespace:LIB0000"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MessageStepView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Button Content="back" VerticalAlignment="Top" Grid.Row="0" Margin="10" Width="125" Command="{Binding cmdNavigateCommand}"/>

        <!--<ui:DataGrid x:Name="Datagrid" ItemsSource="{Binding BasicService.MessagesService.ListOfstepMessages,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"
  Height="770" Margin="10,0,25,10" VerticalAlignment="Top" Grid.Row="1"
  IsManipulationEnabled="True" MinColumnWidth="250" AutoGenerateColumns="False">

            --><!-- Define columns --><!--
            <ui:DataGrid.Columns>
                <DataGridTextColumn Visibility="Hidden" Header="Id" Binding="{Binding Id }" />
                <DataGridTextColumn Header="TimeStamp" Binding="{Binding TimeStamp }" />
                <DataGridTextColumn Header="Group" Binding="{Binding Group }" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status }" />
            </ui:DataGrid.Columns>


            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="Error">
                            <Setter Property="Background" Value="#F44336" />
                            <Setter Property="Foreground" Value="White" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="Warning">
                            <Setter Property="Background" Value="#FF9800" />
                            <Setter Property="Foreground" Value="White" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="Info">
                            <Setter Property="Background" Value="#80B9EE" />
                            <Setter Property="Foreground" Value="Black" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </ui:DataGrid>-->
    </Grid>
</Page>
